# Variables de entorno para Backend (CanchaYa) - PLAN GRATUITO RENDER

# Entorno de ejecución
NODE_ENV=production

# Puerto del servidor (Render usa 10000 automáticamente)
PORT=10000

# URL del frontend para CORS (actualizar con tu URL real)
FRONTEND_URL=https://canchaya-frontend.onrender.com

# Base de datos PostgreSQL EXTERNA (GRATIS)
# Opciones gratuitas: Supabase, Railway, Neon
# Ejemplo Supabase:
DATABASE_URL="postgresql://postgres:[PASSWORD]@db.[PROJECT].supabase.co:5432/postgres"

# Ejemplo Railway:
# DATABASE_URL="postgresql://postgres:[PASSWORD]@[HOST].railway.app:5432/railway"

# Ejemplo Neon:
# DATABASE_URL="postgresql://[USER]:[PASSWORD]@[HOST].neon.tech/[DATABASE]?sslmode=require"

# JWT Secret (opcional si usas autenticación JWT)
# JWT_SECRET="your-secret-key-here"

# Configuración de archivos (si usas almacenamiento de imágenes)
# UPLOAD_PATH="/uploads"
# MAX_FILE_SIZE=5242880