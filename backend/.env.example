# Variables de entorno para Backend (CanchaYa)

# Entorno de ejecución
NODE_ENV=production

# Puerto del servidor (Render usa 10000 por defecto)
PORT=10000

# URL del frontend para CORS
FRONTEND_URL=https://your-frontend-app.onrender.com

# Base de datos PostgreSQL (Render proporcionará automáticamente esta URL)
DATABASE_URL="postgresql://username:password@hostname:port/database_name"

# JWT Secret (opcional si usas autenticación JWT)
# JWT_SECRET="your-secret-key-here"

# Configuración de archivos (si usas almacenamiento de imágenes)
# UPLOAD_PATH="/uploads"
# MAX_FILE_SIZE=5242880