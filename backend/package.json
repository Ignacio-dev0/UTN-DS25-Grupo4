{
  "name": "backend",
  "version": "1.0.1",
  "main": "index.js",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/server.ts",
    "dev:local": "cp .env.local .env && npm run dev",
    "dev:railway": "cp .env.railway .env && npm run dev",
    "build": "npx prisma generate && tsc",
    "start": "echo 'Starting CanchaYa Backend...' && node dist/server.js",
    "start:dev": "node dist/server.js",
    "migrate": "npx prisma migrate deploy",
    "migrate:local": "cp .env.local .env && npx prisma migrate deploy",
    "migrate:railway": "cp .env.railway .env && npx prisma migrate deploy",
    "migrate-data": "ts-node scripts/migrate-data.ts",
    "seed": "ts-node prisma/seed.ts",
    "seed:local": "cp .env.local .env && npm run seed",
    "seed:railway": "cp .env.railway .env && npm run seed",
    "studio:local": "cp .env.local .env && npx prisma studio",
    "studio:railway": "cp .env.railway .env && npx prisma studio",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@getbrevo/brevo": "^3.0.1",
    "@prisma/client": "^6.16.2",
    "@prisma/extension-accelerate": "^2.0.2",
    "@prisma/extension-optimize": "^2.0.0",
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.3",
    "@types/multer": "^2.0.0",
    "@types/node": "^24.3.0",
    "@types/node-cron": "^3.0.11",
    "@types/node-fetch": "^2.6.13",
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "date-fns": "^4.1.0",
    "date-fns-tz": "^3.2.0",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "multer": "^2.0.2",
    "node-cron": "^4.2.1",
    "node-fetch": "^3.3.2",
    "typescript": "^5.9.2",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "@types/bcrypt": "^6.0.0",
    "@types/jest": "^30.0.0",
    "@types/jsonwebtoken": "^9.0.10",
    "@types/supertest": "^6.0.3",
    "jest": "^30.2.0",
    "prisma": "^6.16.2",
    "supertest": "^7.1.4",
    "ts-jest": "^29.4.5",
    "ts-node": "^10.9.2",
    "ts-node-dev": "^2.0.0"
  },
  "prisma": {
    "seed_disabled": "ts-node prisma/seed.ts"
  }
}
